@model CoffeeViewModel

<h2>@Model.Title</h2>

@foreach (var coffee in Model.Coffees)
{
    <div class="col-sm-4 col-lg-4 col-md-4">
        <div class="thumbnail">
            <img src="@coffee.ImageThumbnailUrl" alt="">
            <div class="caption">
                <h3 class="pull-right">@coffee.Price.ToString("c")</h3>
                <h3>
                    <a asp-controller="Coffe" asp-action="Details" asp-route-id="@coffee.Id">@coffee.Name</a>
                </h3>
                <p>@coffee.ShortDescription</p>
            </div>
        </div>
    </div>
}
